import { appendChildren } from './dom-helpers';
import { deleteStorageKey, getStorage, setStorage } from './storage';
import { div, a, image, section, button, main, p, form, input, span } from './tag-create-functions';

export {
  deleteStorageKey,
  getStorage,
  div,
  a,
  image,
  section,
  button,
  main,
  p,
  appendChildren,
  form,
  input,
  setStorage,
  span,
};
